name: firstworkflow

on:
  push:
  branch : main
jobs:
  test-frontend:
    name: it is testing frontend 
    runs-on : ubuntu-latest
    steps:
      -name: checkout code 
       uses: actions/checkout@v2
     
